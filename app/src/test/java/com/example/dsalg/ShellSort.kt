package com.example.dsalg

/**
 * 8.2.2 Shell排序
 * desc:
 * 待排数组array, 长度n = array.size,
 * 增量delta从 n/2 开始，每次
 */
class ShellSort {
    companion object {
        fun <T> shellSort(array: Array<T>) {

        }
    }
}